version: '3'
services:
  movie_dev_1:
    image: tomcat:8-jre8-slim
    restart: always
    networks:
       - legacy
    ports:
       - "8080:8081"
    hostname: "movie_dev_1"
    volumes:
       - ./movie.war:/usr/local/tomcat/webapps/movie.war
    
networks:
    legacy:
      external: true
