version: '3'
services:
  movie_dev:
    image: tomcat
    networks:
       - legacy
    hostname: "movie_dev_1"
    ports:
       - target: 8080
         published: 8081
    volumes:
       - ./movie.war:/usr/local/tomcat/webapps/movie_dev.war
networks:
    legacy:
      external: true
