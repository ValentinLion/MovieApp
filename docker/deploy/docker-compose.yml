version: '3'
services:
  movie_deploy:
    image: tomcat
    networks:
       - legacy
    hostname: "movie_deploy_1"
    ports:
       - target: 8080
       published: 80
    volumes:
       - ./movie.war:/usr/local/tomcat/webapps/movie.war
networks:
    legacy:
      external: true
